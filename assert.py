# quick sanity checking with the "assert" keyword(returns True or False)
user_input = 25

# assert returns an AssertionError if the condition is false
# assert shouldn't be used for validation or in production
assert user_input > 0
